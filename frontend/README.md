# Employee Attendance Management System - Frontend React App

Vite + React frontend application with React Router, Axios, and custom CSS.

## Setup & Running
```bash
npm install
npm run dev
```

App runs on `http://localhost:5173`. Proxies `/api` calls to `http://localhost:5000`.
